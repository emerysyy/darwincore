# ========================================
# DarwinCore 库模块构建配置
# ========================================
# 包含子模块：
# - foundation: 基础功能库（日志、文件、线程、字符串等）
# - network: 网络通信库（TCP 服务器、TCP 客户端）
# ========================================

# add_subdirectory(foundation)
add_subdirectory(network)